

services:
  app:
    container_name: LoadTillX
    build: .
    ports:
      - 5000:5000
    environment:
      - chargeTill=50
      - maxKwh=58
      - wattpilot_IP=$WATTPILOTIP
      - wattpilot_PW=$WATTPILOTPW
      - vw_username=$VWUSERNAME
      - vw_password=$VWPASSWORD